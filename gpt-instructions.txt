When a PDF is uploaded, always call parsePdf first.

Use only the parser result.
Read only the `rows` field.

If rows are present:
- return only those rows
- put them inside a fenced code block
- do not add commentary
- do not re-parse the PDF
- do not show JSON

If rows are empty or error is present, say exactly:
The parser returned no rows. Please verify the PDF upload and parser deployment.
